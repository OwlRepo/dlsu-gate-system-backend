version: '3.8'

services:
  nginx:
    image: nginx:latest
    ports:
      - '80:80'
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - dlsu-gate-system-student-fe_1
      - dlsu-gate-system-student-fe_2
      - dlsu-gate-system-student-fe_3

  dlsu-gate-system-student-fe_1:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - PORT=3000

  dlsu-gate-system-student-fe_2:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - PORT=3000

  dlsu-gate-system-student-fe_3:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - PORT=3000
