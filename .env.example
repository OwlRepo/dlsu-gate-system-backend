# JWT Configuration
JWT_SECRET=your-very-long-secret-key-here  # Secret key for JWT token generation and validation

# Environment Configuration
NODE_ENV=development                        # Application environment (development/production)
PORT=51742                                  # Port number for the NestJS application

# Main PostgreSQL Database Configuration
DB_HOST=localhost                           # PostgreSQL database host
DB_PORT=5432                                # PostgreSQL database port
DB_USERNAME=postgres                        # PostgreSQL username
DB_PASSWORD=your-postgres-password          # PostgreSQL password
DB_NAME=dlsu_gate_system                    # PostgreSQL database name

# Railway Configuration (if using Railway deployment)
DATABASE_URL=your-railway-postgres-url      # Full PostgreSQL connection URL for Railway
RAILWAY_STATIC_URL=your-railway-app-url     # Railway application URL

# Docker Configuration
DOCKER_ENVIRONMENT=true                     # Whether the app is running in Docker

# Source SQL Server Configuration (Student Database)
SOURCE_DB_HOST=202.128.57.226              # SQL Server hostname or IP
SOURCE_DB_PORT=1433                         # SQL Server port (default is 1433)
SOURCE_DB_USERNAME=meraki_login            # SQL Server username
SOURCE_DB_PASSWORD=meraki_password         # SQL Server password
SOURCE_DB_NAME=TEST_ISIDA                  # SQL Server database name

# SQL Server Connection Notes:
# - The SQL Server is accessed through port forwarding
# - Make sure you have network access to the server
# - The server contains the Students table with following columns:
#   * ID_Number (varchar(32))
#   * Name (varchar(99))
#   * Lived_Name (integer)
#   * Remarks (varchar(7))
#   * Photo (varchar(46))
#   * Campus_Entry (varchar(1))
#   * Unique_ID (integer)
#   * isArchived (bit)

# BIOSTAR API Configuration
BIOSTAR_API_BASE_URL=https://202.128.57.226:4430  # BIOSTAR API base URL
BIOSTAR_API_LOGIN_ID=admin                        # BIOSTAR API login username
BIOSTAR_API_PASSWORD=ELIDtech1234                 # BIOSTAR API login password

# Application URLs
BASE_URL=http://localhost                   # Base URL for the application

# Notes:
# 1. Never commit the actual .env file to version control
# 2. Make sure to set proper values in production
# 3. SQL Server connection requires proper network access
# 4. The sync process:
#    - Pulls non-archived students from SQL Server (isArchived = 0)
#    - Syncs to local PostgreSQL database
#    - Converts to CSV
#    - Uploads to BIOSTAR API
# 5. BIOSTAR API endpoints used:
#    - /api/login (POST) - for authentication
#    - /api/users/csv_import (POST) - for uploading student data
# 6. Default sync schedules:
#    - Schedule 1: 09:00 AM daily
#    - Schedule 2: 09:00 PM daily

# Jenkins Configuration
JENKINS_URL=http://localhost:8080
JENKINS_USER=your_jenkins_username
JENKINS_TOKEN=your_jenkins_token
JENKINS_API_TOKEN=your_jenkins_api_token
JENKINS_USERNAME=your_jenkins_username 